//
// Created by jingcoz on 9/14/17.
//

# include "Draw.h"

using namespace pba;
using namespace std;

void Draw::draw_scene()
{
//    size_t face_count = faces.size();
//    GLuint *faces_list;
//    GLfloat *vertices_list;
//    GLfloat *colors_list;
//
//    faces_list = (GLuint *)calloc(box_face_num * 4, 3 * sizeof(GLuint));
//
//    // draw
//    glEnableClientState(GL_VERTEX_ARRAY);
//    glEnableClientState(GL_NORMAL_ARRAY);
//    glEnableClientState(GL_COLOR_ARRAY);
//    glVertexPointer(3, GL_FLOAT, 3 * sizeof(GLfloat), vertices_list);
//    glNormalPointer(GL_FLOAT, 3 * sizeof(GLfloat), box_normals);
//    glColorPointer(3, GL_FLOAT, 3 * sizeof(GLfloat), colors_list);
//    glDrawElements(GL_TRIANGLES, int(3 * face_count), GL_UNSIGNED_INT, faces_list);
}
